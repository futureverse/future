# Creates a connection to the system null device

Creates a connection to the system null device

## Usage

``` r
nullcon()
```

## Value

Returns a open, binary
[`base::connection()`](https://rdrr.io/r/base/connections.html).
