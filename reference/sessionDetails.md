# Outputs details on the current R session

Outputs details on the current R session

## Usage

``` r
sessionDetails(env = FALSE)
```

## Arguments

- env:

  If TRUE, [`Sys.getenv()`](https://rdrr.io/r/base/Sys.getenv.html)
  information is returned.

## Value

Invisibly a list of all details.
